<% breadcrumb ["Companies", companies_path], [@company[:name], company_path(@company[:id])], "Edit" %>

<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-gray-900">Edit Company</h1>
    <p class="mt-1 text-sm text-gray-600">Update company information</p>
  </div>

  <%= render 'form', company: @company %>
</div>