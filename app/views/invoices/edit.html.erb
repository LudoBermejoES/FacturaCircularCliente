<% breadcrumb ["Invoices", invoices_path], [@invoice[:invoice_number], invoice_path(@invoice[:id])], "Edit" %>

<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-gray-900">Edit Invoice</h1>
    <p class="mt-1 text-sm text-gray-600">Update invoice <%= @invoice[:invoice_number] %></p>
  </div>

  <%= render 'form', invoice: @invoice %>
</div>