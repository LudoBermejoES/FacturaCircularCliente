<%= turbo_stream.replace "invoice_status" do %>
  <span class="<%= status_badge_class(@invoice[:status]) %>">
    <%= @invoice[:status].humanize %>
  </span>
<% end %>

<%= turbo_stream.replace "workflow_content" do %>
  <turbo-frame id="workflow_content">
    <!-- Update available transitions -->
    <% if @available_transitions.present? %>
      <div class="bg-white shadow rounded-lg p-6">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Available Transitions</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          <% @available_transitions.each do |transition| %>
            <div class="border border-gray-200 rounded-lg p-4">
              <div class="flex items-center justify-between mb-3">
                <span class="text-sm font-medium text-gray-900">
                  <%= transition[:to_status_name] || transition[:to_status].humanize %>
                </span>
                <span class="<%= status_badge_class(transition[:to_status]) %>">
                  <%= transition[:to_status] %>
                </span>
              </div>
              
              <%= form_with url: transition_invoice_workflow_path(@invoice[:id]),
                            method: :post,
                            data: { turbo_frame: "workflow_content" },
                            class: "space-y-3" do |f| %>
                <%= hidden_field_tag :status, transition[:to_status] %>
                
                <div>
                  <%= f.text_area :comment,
                                  placeholder: transition[:requires_comment] ? "Add a comment (required)" : "Add a comment (optional)",
                                  required: transition[:requires_comment],
                                  rows: 2,
                                  class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
                </div>
                
                <%= f.submit "Transition to #{transition[:to_status_name] || transition[:to_status].humanize}",
                            class: "w-full inline-flex justify-center rounded-md border border-transparent bg-indigo-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2",
                            data: {
                              confirm: "Are you sure you want to change the status to #{transition[:to_status_name] || transition[:to_status]}?",
                              turbo_confirm: "Are you sure?"
                            } %>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
    
    <!-- Update workflow history -->
    <div class="bg-white shadow rounded-lg p-6 mt-6">
      <h2 class="text-lg font-semibold text-gray-900 mb-4">Workflow History</h2>
      
      <% if @history.present? %>
        <div class="flow-root">
          <ul role="list" class="-mb-8">
            <% @history.each_with_index do |event, index| %>
              <li>
                <div class="relative pb-8">
                  <% if index != @history.length - 1 %>
                    <span class="absolute left-4 top-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                  <% end %>
                  
                  <div class="relative flex space-x-3">
                    <div>
                      <span class="h-8 w-8 rounded-full flex items-center justify-center ring-8 ring-white bg-indigo-500">
                        <svg class="h-5 w-5 text-white" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 2a1 1 0 00-1 1v1a1 1 0 002 0V3a1 1 0 00-1-1zM4 4h3a3 3 0 006 0h3a2 2 0 012 2v9a2 2 0 01-2 2H4a2 2 0 01-2-2V6a2 2 0 012-2zm2.5 7a1.5 1.5 0 100-3 1.5 1.5 0 000 3zm2.45 4a2.5 2.5 0 10-4.9 0h4.9zM12 9a1 1 0 100 2h3a1 1 0 100-2h-3zm-1 4a1 1 0 011-1h2a1 1 0 110 2h-2a1 1 0 01-1-1z" clip-rule="evenodd"/>
                        </svg>
                      </span>
                    </div>
                    
                    <div class="flex min-w-0 flex-1 justify-between space-x-4 pt-1.5">
                      <div>
                        <p class="text-sm text-gray-900">
                          Status changed from 
                          <span class="font-medium"><%= event[:from_status] %></span>
                          to
                          <span class="font-medium"><%= event[:to_status] %></span>
                        </p>
                        
                        <% if event[:comment].present? %>
                          <p class="mt-1 text-sm text-gray-600">
                            <%= event[:comment] %>
                          </p>
                        <% end %>
                        
                        <% if event[:user_name].present? %>
                          <p class="mt-1 text-xs text-gray-500">
                            by <%= event[:user_name] %>
                          </p>
                        <% end %>
                      </div>
                      
                      <div class="whitespace-nowrap text-right text-sm text-gray-500">
                        <time datetime="<%= event[:created_at] %>">
                          <%= format_date_short(event[:created_at]) %>
                        </time>
                      </div>
                    </div>
                  </div>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>
    </div>
  </turbo-frame>
<% end %>

<%= turbo_stream.append "toast_container" do %>
  <div data-controller="toast" 
       data-toast-message-value="Status successfully updated to <%= @invoice[:status] %>"
       class="hidden"></div>
<% end %>