<% breadcrumb ["Companies", companies_path], [@company[:name], company_path(@company[:id])], ["Invoice Series", company_invoice_series_index_path(@company[:id])], [@invoice_series[:series_name], company_invoice_series_path(@company[:id], @invoice_series[:id])], "Edit" %>

<div class="max-w-4xl mx-auto">
  <div class="bg-white shadow rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h1 class="text-2xl font-bold text-gray-900 mb-6">Edit Invoice Series</h1>
      <%= render 'form' %>
    </div>
  </div>
</div>