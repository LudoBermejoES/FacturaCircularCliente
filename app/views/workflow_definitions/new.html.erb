<div class="container mx-auto px-4 py-6">
  <div class="flex items-center mb-6">
    <%= link_to workflow_definitions_path, class: "text-gray-600 hover:text-gray-800 mr-4" do %>
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
    <% end %>
    <h1 class="text-3xl font-bold text-gray-900">New Workflow Definition</h1>
  </div>

  <% if flash[:error] || flash.now[:error] %>
    <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
      <%= flash[:error] || flash.now[:error] %>
    </div>
  <% end %>

  <div class="bg-white shadow-sm rounded-lg p-6">
    <%= form_with scope: :workflow_definition, local: true, url: workflow_definitions_path, class: "space-y-6" do |form| %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <%= form.label :name, class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :name,
              class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500",
              placeholder: "e.g. Standard Invoice Workflow" %>
        </div>

        <div>
          <%= form.label :code, class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :code,
              class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500",
              placeholder: "e.g. INVOICE_STANDARD" %>
        </div>
      </div>

      <div class="grid grid-cols-1 gap-6">
        <div>
          <%= form.label :company, "Company", class: "block text-sm font-medium text-gray-700" %>
          <%= form.text_field :company,
              value: current_company&.dig('name') || current_company&.dig(:name) || "No company assigned",
              readonly: true,
              class: "mt-1 block w-full rounded-md border-gray-300 bg-gray-50 shadow-sm text-gray-700",
              placeholder: "Company will be automatically assigned" %>
          <%= form.hidden_field :company_id, value: current_company_id %>
          <p class="mt-1 text-sm text-gray-500">Automatically assigned from your current company</p>
        </div>
      </div>

      <div>
        <%= form.label :description, class: "block text-sm font-medium text-gray-700" %>
        <%= form.text_area :description,
            rows: 3,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500",
            placeholder: "Describe the purpose and process of this workflow..." %>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="flex items-center">
          <%= form.check_box :is_active,
              class: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
          <%= form.label :is_active, "Active", class: "ml-2 block text-sm text-gray-900" %>
          <p class="mt-1 text-sm text-gray-500">Enable this workflow for use</p>
        </div>

        <div class="flex items-center">
          <%= form.check_box :is_default,
              class: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" %>
          <%= form.label :is_default, "Default Template", class: "ml-2 block text-sm text-gray-900" %>
          <p class="mt-1 text-sm text-gray-500">Make this the default workflow template</p>
        </div>
      </div>

      <div class="flex justify-end space-x-3">
        <%= link_to workflow_definitions_path,
            class: "px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" do %>
          Cancel
        <% end %>
        <%= form.submit "Create Workflow Definition",
            class: "px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700" %>
      </div>
    <% end %>
  </div>

  <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">Next Steps</h3>
        <div class="mt-2 text-sm text-blue-700">
          <p>After creating the workflow definition, you'll be able to:</p>
          <ul class="list-disc list-inside mt-1">
            <li>Add states to define the workflow stages</li>
            <li>Create transitions between states</li>
            <li>Set up roles and permissions for each transition</li>
            <li>Configure guard conditions and actions</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>